# Public assets — placeholders to replace

These files are referenced by the site. Swap them for the real thing; no code
changes needed unless you rename a file (then update `lib/content.ts`).

| File | Used for | What to do |
| --- | --- | --- |
| `headshot.jpg` | Hero portrait | Add a square photo here (a clean monogram placeholder shows until you do). |
| `0626-CV-AbdullahAnjrini.pdf` | "Download CV" links | The real CV (referenced by `links.cv` in `lib/content.ts`). |

Also update these in [`lib/content.ts`](../lib/content.ts):

- `links.github` — your GitHub profile URL
- `links.linkedin` — your LinkedIn profile URL

Email (`abdullahanjarini@gmail.com`) is already wired up.
